steps:
  # build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build', 
    '-t', 
    'gcr.io/$PROJECT_IDt/est_project', 
    '.'
    ]
  # push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/test_project']
  # Deploy container image to Cloud Run
- name: 'gcr.io/cloud-builders/gcloud'
  args: [
    'beta',
    'run',
    'deploy',
    '--allow-unauthenticated',
    'test_project',
    '--image',
    'gcr.io/$PROJECT_ID/test_project',
    '--region',
    'us-central1',
    '--platform',
    'managed',
    '--memory',
    '256Mi',
    '--update-env-vars',
    'APP_DEBUG=${_APP_DEBUG}'
    ]
images:
- gcr.io/$PROJECT_ID/test_project

options:
    substitution_option: 'ALLOW_LOOSE'

substitutions:
    _APP_DEBUG: 'true'
